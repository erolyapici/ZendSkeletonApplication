<?php
$title = 'Add new album';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<form method="post" action="<?php echo $this->url('album',array('action'=>'add'));?>">
    <input type="hidden" name="id"  id="id" value="">

    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" class="form-control" name="title" id="title" placeholder="Title">
        <?php
        if($isPost) {
            echo $this->formElementErrors($form->get('title'));
        }?>
    </div>
    <div class="form-group">
        <label for="artist">Artist</label>
        <input type="text" class="form-control" name="artist" id="artist" placeholder="Artist">
        <?php
        if($isPost) {
            echo $this->formElementErrors($form->get('artist'));
        }
        ?>
    </div>
    <button type="submit" class="btn btn-default">Submit</button>

</form>